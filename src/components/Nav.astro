---
---
<nav>
  <div class="nav-inner">
    <a href="/" class="nav-logo">🐣 Chickentrepreneur</a>
    <div class="nav-right">
      <a href="/about" class="btn-pill">About</a>
      <div class="dropdown-wrapper">
        <button class="btn-pill btn-ghost" tabindex="0">Learn ↓</button>
        <div class="dropdown-menu">
          <a href="/">🏠 Home</a>
          <div class="dropdown-divider"></div>
          <a href="/category/mindset">🧠 Mindset & Soft Skills</a>
          <a href="/category/frameworks">🗺️ Frameworks</a>
          <a href="/category/gtm">📈 GTM</a>
          <a href="/category/tools">🛠️ Tools & Tech</a>
          <a href="/category/career">💼 Career</a>
        </div>
      </div>
    </div>
  </div>
</nav>

<style>
  nav {
    background: #ffffff;
    border-bottom: 1px solid rgba(0,0,0,0.07);
    padding: 0 2rem;
    position: sticky;
    top: 0;
    z-index: 200;
  }
  .nav-inner {
    max-width: 960px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 58px;
  }
  .nav-logo {
    font-family: 'Instrument Serif', serif;
    font-size: 19px;
    color: #0a0a0a;
    font-weight: 400;
    text-decoration: none;
    letter-spacing: 0.01em;
  }
  .nav-right { display: flex; gap: 0.5rem; align-items: center; }
  .btn-pill {
    font-family: 'Inter', sans-serif;
    font-size: 13px;
    padding: 5px 16px;
    border-radius: 999px;
    cursor: pointer;
    border: 1px solid rgba(0,0,0,0.15);
    background: transparent;
    color: #0a0a0a;
    text-decoration: none;
    transition: background 0.12s, border-color 0.12s;
    white-space: nowrap;
  }
  .btn-pill:hover { background: #f5f5f5; border-color: rgba(0,0,0,0.25); }
  .btn-ghost { background: transparent; border-color: rgba(0,0,0,0.1); color: #0a0a0a; }
  .btn-ghost:hover { background: #f5f5f5; border-color: rgba(0,0,0,0.25); color: #0a0a0a; }

  .dropdown-wrapper { position: relative; }
  .dropdown-menu {
    visibility: hidden; opacity: 0; pointer-events: none;
    transition: opacity 0.12s, visibility 0.12s;
    position: absolute; right: 0; top: 100%;
    background: #ffffff;
    border: 1px solid rgba(0,0,0,0.08);
    border-radius: 10px; padding: 6px; min-width: 210px;
    box-shadow: 0 8px 32px rgba(0,0,0,0.08);
    margin-top: 4px;
  }
  .dropdown-wrapper:hover .dropdown-menu,
  .dropdown-wrapper:focus-within .dropdown-menu {
    visibility: visible; opacity: 1; pointer-events: auto;
  }
  .dropdown-menu a {
    display: block; padding: 7px 12px;
    font-family: 'Inter', sans-serif; font-size: 13px;
    color: #0a0a0a; border-radius: 6px; text-decoration: none;
    transition: background 0.1s;
  }
  .dropdown-menu a:hover { background: rgba(166,209,255,0.15); color: #0a0a0a; }
  .dropdown-divider { height: 0.5px; background: rgba(0,0,0,0.07); margin: 4px 8px; }
</style>
